PROMPT.md + CONTEXT.md + docs/LLM_RULES.md 준수(SSOT).
우선순위: TDD(테스트 먼저) > 성능(가상화/캐시/프리패치) > 정량지표(수집→저장→조회) > 디자인시스템(tokens/ui/storybook).
스택 변경 금지(변경 필요 시 ADR 먼저).
산출물: 변경 파일 목록 + 핵심 코드 + 테스트 + 실행 방법.
